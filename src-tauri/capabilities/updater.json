{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Self-update. The banner that drives it lives in the sidebar, so only the main window may check, download or install a release.",
  "windows": ["main"],
  "permissions": ["updater:default"]
}
